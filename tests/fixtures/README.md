# Test fixtures

`smoke_test.ppm` is the fixed local image used by model smoke tests. It is a small,
synthetic RGB color grid stored as text so the fixture is deterministic and reviewable.
Smoke tests measure whether inference completes; they do not evaluate prediction quality.
